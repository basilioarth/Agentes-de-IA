# Resumo da Aula – Model Context Protocol (MCP)

## O que é o MCP?
**Model Context Protocol (MCP)** é um **protocolo open source criado pela Anthropic** (empresa por trás do Claude) com o objetivo de padronizar a forma como **IAs (especialmente agentes) se conectam a sistemas, ferramentas e recursos externos**.

### Definição simples
> MCP = um conjunto de regras de comunicação (um “idioma” padrão) que permite que qualquer IA converse com qualquer servidor externo que também fale esse mesmo idioma.

### Analogia perfeita do professor
Um protocolo é como as regras sociais implícitas em um jantar na casa de um amigo:
- Você leva algo (vinho, sobremesa)  
- Fica conversando, mas não até muito tarde  
- Quando diz “vou embora”, o anfitrião insiste um pouco, você recusa educadamente  

Todos sabem o que esperar porque seguem o **mesmo protocolo**.  
Se alguém quebrar (ex.: responder “especiarias” quando perguntam a forma de pagamento), a comunicação trava.

O MCP faz exatamente isso entre **IA** e **servidores externos**.

## Problema que o MCP resolve
**No mundo sem MCP**:
- Para cada ferramenta externa (Gmail, Google Calendar, busca na web, banco de dados local etc.), o desenvolvedor precisava:
  - Escrever código específico (HTTP calls, autenticação, parsing de JSON etc.)
  - Definir manualmente como a IA iria usar aquela ferramenta
  - Criar descrições individuais para cada função
- Resultado: muito trabalho repetitivo, integração frágil e difícil de escalar

**Com o MCP**:
- Você cria (ou usa um já existente) um **MCP Server** para cada recurso  
- Todos os MCP Servers **falam a mesma língua** (seguem o mesmo protocolo)  
- A IA só precisa saber falar MCP → consegue acessar **qualquer** recurso que tenha um MCP Server

## O que um MCP Server pode expor?
Um MCP Server funciona como um **catálogo padronizado** que oferece três tipos de coisas:

1. **Ferramentas (tools/functions)**  
   - Ex.: ler e-mails, consultar agenda, buscar na internet, editar arquivos  
   - Inclui metadados: nome, descrição, parâmetros de entrada/saída

2. **Prompts prontos**  
   - Instruções especializadas que transformam a IA em “expert” em algo  
   - Ex.: “prompt de estudo de programação da Rocketseat” → a IA vira tutor automaticamente sem você precisar copiar o prompt

3. **Recursos/dados**  
   - Arquivos, bancos de dados, textos, áudio etc.  
   - A IA pode consultar diretamente sem download ou processamento manual

## Por que o MCP é revolucionário?
- **Padronização total**: uma IA pode acessar milhares de recursos diferentes sem código específico para cada um  
- **Benefício mútuo**:  
  - Empresas que criam MCP Servers ganham tráfego (todas as IAs podem usar seus serviços)  
  - IAs ficam muito mais poderosas (acesso fácil a tudo)  
  - Desenvolvedores não precisam mais reinventar a roda para cada integração
- **Open source**: qualquer pessoa pode implementar um MCP Server ou conectar sua IA a um

## Exemplo prático comparativo

**Sem MCP**  
IA → código personalizado → Gmail API → parse → extrai info → devolve à IA  
IA → código personalizado → app de agenda → parse → devolve à IA  
(Repete para cada ferramenta)

**Com MCP**  
IA → fala MCP → MCP Server do Gmail (feito pelo Google)  
IA → fala MCP → MCP Server do Calendar (feito por terceiros)  
IA → fala MCP → seu próprio MCP Server local com ferramentas customizadas  

Tudo padronizado. Nenhum código específico por ferramenta.

## Status atual (2025)
- Muito novo: lançado pela Anthropic há cerca de 6–8 meses  
- Empresas grandes (Google, Meta, OpenAI etc.) já estão adotando  
- Comunidade começando a criar MCP Servers  
- Estamos “na crista da onda” — tema extremamente quente e em rápida evolução

## Próximo passo da aula (hands-on)
- Criar nosso **próprio MCP Server** para expor funções locais  
- Conectar o **Gemini** (o mesmo usado na secretária) a esse servidor  
- Ver na prática como a IA consome ferramentas, prompts e recursos via MCP

O MCP tem potencial de ser **tão transformador quanto o próprio ReAct + Tool Use** — ele é o “HTTP dos agentes de IA”.  
Agora vamos codar um MCP Server do zero! 🚀