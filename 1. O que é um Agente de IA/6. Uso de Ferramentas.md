# Resumo da Aula – Uso de Ferramentas (Tool Use) em Agentes de IA

## O que são ferramentas (tools)?
- Ferramentas são **funções em código** que permitem à IA realizar ações reais no mundo (digital ou físico)  
- Sem ferramentas, um LLM só gera texto → não é um agente  
- Com ferramentas, o LLM vira um **agente autônomo** capaz de atuar

## Como as ferramentas funcionam no agente?
1. **Catálogo de ferramentas**  
   - Lista de funções disponíveis  
   - Cada ferramenta tem:  
     - Nome da função  
     - Descrição do que ela faz  
     - Parâmetros de entrada (inputs)  
     - O que retorna (output)

2. **Processo de decisão da IA**  
   - A IA recebe a solicitação do usuário  
   - Usa o **raciocínio (Reasoning)** para analisar o catálogo  
   - Identifica qual ferramenta é a mais adequada  
   - Decide os parâmetros corretos a partir do contexto da solicitação  
   - Indica qual ferramenta deve ser chamada (e com quais argumentos)

## Exemplo prático dado pelo professor
Usuário pede: “Toca a música Sweet Caroline”

A IA raciocina:
- Analisa o catálogo de ferramentas disponíveis:  
  1. `getWeather(location, time)` → retorna temperatura (não serve)  
  2. `turnLightsOn(list_of_light_ids)` → liga luzes (não serve)  
  3. `playMusic(song_name)` → toca música no dispositivo (é essa!)

- Decide:  
  - Usar a ferramenta `playMusic`  
  - Passar o parâmetro `song_name = "Sweet Caroline"`  

- O sistema (não a LLM diretamente) executa a função → música começa a tocar  
- IA pode responder: “Já está tocando!”

## Importante: quem executa a ferramenta?
- A **LLM (ChatGPT, Gemini etc.) não executa** a função diretamente  
- A LLM apenas **prevê/decide** qual ferramenta chamar e com quais parâmetros  
- Quem realmente chama e executa a função é o **sistema do agente** (o código que envolve a LLM)  
- Em plataformas como ChatGPT/Gemini com tools nativas, o próprio sistema faz essa execução automaticamente

## O agente de IA é o conjunto completo
- Agente = LLM generativa + Catálogo de ferramentas + Sistema que executa as chamadas  
- Sem o catálogo e o executor de ferramentas → não há agente, só um chatbot comum

## Exemplos de ferramentas comuns
- `searchInternet(query)` → busca na web  
- `getWeather(...)` → consulta clima  
- `editCode(...)` → altera código em repositório  
- `createPullRequest(...)` → abre PR no GitHub  
- `playMusic(...)` → controla dispositivo de áudio  
- E muitas outras que veremos na prática

## Conclusão do professor
O uso de ferramentas é o conceito **mais fundamental** para transformar um LLM em um agente de verdade.  
Combinado com o paradigma ReAct (raciocinar antes de atuar), é isso que permite autonomia real.

Próximo passo da aula: vamos criar nosso primeiro agente do zero, declarar ferramentas e ver tudo isso funcionando na prática! 🚀