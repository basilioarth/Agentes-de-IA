# Resumo da Aula – Declaração de Todas as Ferramentas da Secretária

## Organização do código
- Criada pasta `tools/` com dois arquivos:  
  - `calendar.js` → funções relacionadas a agenda  
  - `email.js` → funções relacionadas a e-mails  
- Todas as funções são **declaradas primeiro** (nome, descrição, parâmetros) antes de serem implementadas (boa prática para definir a interface claramente)

## Ferramentas declaradas (total de 6)

### Calendário (4 funções)

1. **`getTodayDate`**  
   - Descrição: Retorna a data de hoje no formato YYYY-MM-DD  
   - Parâmetros: nenhum  

2. **`getEvents`**  
   - Descrição: Retorna os eventos do calendário para um determinado dia  
   - Parâmetros:  
     - `date` (string, required): data no formato YYYY-MM-DD  
   - `required: ["date"]`

3. **`scheduleEvent`**  
   - Descrição: Marca um novo evento na agenda  
   - Parâmetros:  
     - `title` (string, required): título do evento  
     - `date` (string, required): data no formato YYYY-MM-DD  
     - `time` (string, required): hora no formato HH:MM  
     - `attendees` (array de strings, opcional): lista de nomes dos convidados  
   - `required: ["title", "date", "time"]`

4. **`rescheduleEvent`**  
   - Descrição: Remarca um evento existente para um novo horário  
   - Parâmetros:  
     - `title` (string, required): título do evento a remarcar  
     - `date` (string, required): data do evento (YYYY-MM-DD)  
     - `newTime` (string, required): nova hora (HH:MM)  
   - `required: ["title", "date", "newTime"]`

### E-mail (2 funções)

5. **`getEmails`**  
   - Descrição: Retorna todos os e-mails na caixa de entrada  
   - Parâmetros: nenhum  

6. **`sendEmail`**  
   - Descrição: Envia um e-mail para um contato  
   - Parâmetros:  
     - `contact` (string, required): nome/e-mail do destinatário  
     - `message` (string, required): corpo da mensagem  
   - `required: ["contact", "message"]`

## Integração no código principal
- Cada arquivo exporta sua lista de declarações  
- No `secretaria.js`:  
  ```js
  import { calendarFunctions } from './tools/calendar.js';
  import { emailFunctions } from './tools/email.js';
  
  const allFunctions = [...calendarFunctions, ...emailFunctions];
  ```
- Passado para a API do Gemini via `functionDeclarations: allFunctions`

## Testes de declaração (a IA entendeu tudo corretamente!)

1. **"Qual o dia de hoje?"**  
   → Chamou `getTodayDate` (sem argumentos)

2. **"Quais os eventos na minha agenda para o dia 10 de 06 de 2037?"**  
   → Chamou `getEvents` com `date: "2037-06-10"`  
   → Converteu automaticamente do formato brasileiro para YYYY-MM-DD

3. **"Marque um evento novo para o dia 31/12/2026 chamado Ano Novo com a Camila e o Jorge às 22 horas"**  
   → Chamou `scheduleEvent` com:  
     - `title: "Ano Novo"`  
     - `date: "2026-12-31"`  
     - `time: "22:00"` (ou similar)  
     - `attendees: ["Camila", "Jorge"]`

4. **"Mande uma mensagem bonita de aniversário para a minha mãe"**  
   → Perguntou o contato → Após resposta "mãe"  
   → Chamou `sendEmail` com `contact: "mãe"` e uma mensagem bonita de aniversário

## Observações finais do professor
- Declarar as funções antes de implementá-las ajuda a definir bem a interface  
- A IA está convertendo formatos de data corretamente com base na descrição  
- O comportamento de seleção de função e preenchimento de parâmetros está funcionando perfeitamente

## Próximo passo
- **Implementar** todas as 6 funções (com dados mockados)  
- Criar a lógica para:  
  - Detectar qual função foi chamada (pelo `name`)  
  - Executar a função correspondente com os argumentos recebidos  
  - Devolver o resultado via `functionResponse`  
- Testar a secretária completa em conversas reais no terminal

As ferramentas estão declaradas e a IA já as entende perfeitamente! Agora é hora de dar vida à secretária com implementações reais (mesmo que mockadas). 🚀