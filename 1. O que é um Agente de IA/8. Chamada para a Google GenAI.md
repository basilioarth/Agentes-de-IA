# Resumo da Aula – Início da Implementação da Secretária de IA

## Roadmap do projeto hands-on
1. **Criar o ambiente e fazer a primeira chamada à IA** (feito nesta aula)  
2. Testar chamadas de funções (tools)  
3. Declarar as funções/ferramentas da secretária  
4. Implementar todas as funções (consultar data, calendário, e-mails etc.)  
5. Testar o agente completo  

## Parênteses importantes do professor
- **Modelo usado**: Gemini (via API do Google)  
  - Tem tier gratuito suficiente para testes  
  - Limite de requisições; pode cadastrar cartão para aumentar (só cobra se exceder)  
- **Não vamos usar APIs reais de calendário/e-mail**  
  - Tudo será **mockado** (falso): calendário fake e e-mail fake  
  - Motivos:  
    - Foge do escopo do módulo (foco em agentes e tools)  
    - Cada serviço real (Google Calendar, Gmail, Outlook etc.) tem API diferente  
  - Fica como exercício para quem quiser expandir depois  
- **Interface**: sem UI de chat → vamos usar o **terminal** como conversa (mais rápido e direto)

## Configuração inicial do projeto
- `npm init` → cria o projeto Node.js  
- `npm add google-generative-ai` → instala o SDK oficial do Gemini  
- Cria arquivo `secretaria.js`  
- Configura instância do Gemini:  
  ```js
  const ai = new GoogleGenerativeAI(process.env.GOOGLE_GEN_AI_API_KEY);
  // ou diretamente com string da chave
  ```
- Primeiro teste: chamada simples ao modelo **Gemini 1.5 Flash**

## Estrutura da chamada à API
```js
const response = await ai
  .getGenerativeModel({ model: "gemini-1.5-flash" })
  .generateContent({
    contents: [
      {
        role: "user",
        parts: [{ text: "Que dia é hoje?" }]
      }
    ]
  });
```

- `contents`: lista de mensagens da conversa (histórico)  
- Cada mensagem tem `role` ("user" ou "model") e `parts` (neste caso, texto)

## Resultado do teste
Pergunta: “Que dia é hoje?”  
Resposta do Gemini: **“Hoje é terça-feira, 23 de abril de 2024”**  

**Errado em todos os aspectos** (data real da aula é outra, provavelmente em 2025).

## Por que a IA errou?
- LLMs são uma **caixa preta** sem acesso direto ao mundo externo  
- Não sabem a data/hora atual → apenas geram texto plausível com base no treinamento  
- Quando usamos a API pura (não a interface web), **não há ferramenta automática de data** como no chat oficial

## Próximo passo (próxima aula)
- Dar à IA a capacidade de **consultar a data real**  
- Implementar a **primeira ferramenta (tool/function calling)** para obter a data atual  
- Assim, o agente conseguirá responder corretamente perguntas sobre o dia de hoje

O projeto começou! Agora temos o ambiente pronto e vimos na prática por que precisamos de ferramentas para tornar o agente útil. 🚀